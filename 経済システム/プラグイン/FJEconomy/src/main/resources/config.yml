# FJ Economy Plugin Configuration

# 設定ファイルのスキーマバージョン（プラグインが自動で更新します。手動で変更しないでください）
config-version: 1

# Database settings
database:
  # Connection URL
  url: "jdbc:mariadb://10.2.1.27:3306/fjeconomy?characterEncoding=utf8mb4&serverTimezone=Asia/Tokyo"
  username: "fjeconomy"
  password: "your_password_here"
  
  # Connection pool settings
  pool_size: 10
  max_lifetime: 1800000  # 30 minutes in milliseconds

# Server identification
server:
  # Server ID (mc1, mc2, mc3)
  id: "mc1"
  # Server name for display
  name: "Main Server"

# Currency settings
currency:
  # Currency symbol
  symbol: "¥"
  # Currency name
  name: "FJ Credits"
  # Decimal places (0 = integer only)
  decimal_places: 0

# Tax and fees
economy:
  # Tax rate on shop transactions (percentage, e.g., 10 = 10%)
  tax_rate: 10.0
  # Rounding method: "HALF_UP" or "DOWN"
  rounding_method: "HALF_UP"
  # Initial balance for new players
  starting_balance: 1000
  # Allow negative balances (debt)
  allow_negative: false

# Shop system
shop:
  # Default shop stock (per item)
  default_stock: 100
  # Enable shop price updates from web
  sync_prices: true
  # Sync interval in seconds
  sync_interval: 300

# Government account
government:
  # Government account UUID
  uuid: "00000000-0000-0000-0000-000000000001"
  # Government account name
  name: "GOVERNMENT"

# Web API settings
web_api:
  # Enable web API endpoints
  enabled: true
  # API base URL
  base_url: "https://clusters-prj.com/api/economy"
  # API token (change this!)
  token: "your_api_token_here"

# Logging
logging:
  # Log level: DEBUG, INFO, WARN, ERROR
  level: "INFO"
  # Log file location
  file: "plugins/FJEconomy/logs/economy.log"
  # Log transactions
  log_transactions: true

# Messages
messages:
  prefix: "<gray>[<aqua>ふじゅ～ペイシステム<gray>] "
  
  # Command messages
  balance_format: "{player} の残高: {symbol}{balance}"
  balance_insufficient: "&c残高不足です"
  payment_success: "&a{amount}{symbol} を {receiver} に送金しました"
  payment_received: "&a{sender} から {amount}{symbol} を受け取りました"
  
  # Shop messages
  purchase_success: "&a{item}を{amount}個購入しました（支払額: {symbol}{total}）"
  purchase_failed: "&c購入に失敗しました: {reason}"
  shop_info: "&b{npc}の店&r - {item}: {symbol}{price}（在庫: {stock}個）"
  
  # Admin messages
  give_success: "&a{player} に {amount}{symbol} を付与しました"
  take_success: "&a{player} から {amount}{symbol} を没収しました"
  set_success: "&a{player} の残高を {amount}{symbol} に設定しました"
  
  # Error messages
  player_not_found: "&cプレイヤーが見つかりません: {player}"
  database_error: "&cデータベースエラー: {error}"
  invalid_amount: "&c無効な金額です"
  reload_success: "&aFJ Economy をリロードしました"
  reload_failed: "&cリロードに失敗しました: {error}"

# ログインボーナス設定
login_bonus:
  enabled: true
  amount: 500
  cooldown_hours: 24

# 建築量ポイント設定（CoreProtect のログを集計してポイントを付与する）
# WorldEdit による設置も CoreProtect 側の config.yml で worldedit: true になっていれば集計対象になる
build_reward:
  enabled: true
  # 集計間隔（時間）。0時を起点に区切るため 24 の約数（1/2/3/4/6/8/12/24）を指定すること
  interval_hours: 3
  # 設置1ブロックあたりのポイント
  points_per_block: 1
  # 破壊したブロック数をスコアから差し引くか（設置→破壊の繰り返し稼ぎ対策）
  subtract_breaks: true
  # この設置数未満の期間はポイントを付与しない（ログには残る）
  min_blocks: 10
  # 1期間あたりの上限ポイント（0で無制限）
  max_points_per_period: 3000
  # ポイントの原資を国庫から支出するか（false の場合は新規発行）
  from_government: true
  # 集計から除外するブロック（例: [DIRT, COBBLESTONE]）
  excluded_materials: []
  # 集計対象にするプレイヤー数の上限（fje_balances の最終更新が新しい順）
  max_players: 500
  # Web管理画面からの集計リクエストを拾う間隔（秒）
  query_poll_seconds: 10
  # Web管理画面から遡れる最大日数
  max_lookup_days: 30

# Webアカウント連携設定
account_link:
  # 連携コードの有効期限（分）
  code_expiry_minutes: 10
  # 連携先WebサイトのURL（案内メッセージ表示用）
  website_url: "https://fjew.clusters-prj.com/"